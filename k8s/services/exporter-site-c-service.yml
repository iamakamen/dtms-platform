apiVersion: v1
kind: Service
metadata:
  name: exporter-site-c
  labels:
    app: exporter-site-c
spec:
  selector:
    app: exporter-site-c
  ports:
    - name: http-metrics
      port: 8000
      targetPort: 8000
  type: ClusterIP
