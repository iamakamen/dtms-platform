apiVersion: v1
kind: Service
metadata:
  name: exporter-site-a
  labels:
    app: exporter-site-a
spec:
  selector:
    app: exporter-site-a
  ports:
    - name: http-metrics
      port: 8000
      targetPort: 8000
  type: ClusterIP
